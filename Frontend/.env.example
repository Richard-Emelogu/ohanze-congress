# Replace this with the public URL of your backend API.
# If you deploy your backend to a service like Railway, Render, or another host,
# point this to the deployed backend base URL.
REACT_APP_API_URL=https://your-backend-host.com/api
